export const MAX_RETRY_QUERIES = 3;

export const SCREEN_SIZE = {
  MOBILE: 768,
  TABLET: 1024,
  DESKTOP: 1440,
};

export const ICON_SIZE_BOTTOM_NAVIGATION = 24;
export enum PRICE_TYPE_ENUM {
  FIXED = 'FIXED',
  PERCENT = 'PERCENT',
}

export const DEFAULT_ID = -1;

export const SEPARATOR_POLICY = '::';

export const HEADER_TEAM_ID = 'x-team-id';

export const defaultPrefixCls = 'no-blogg';

// User
export enum InviteType {
  PRIVATE = 0,
  PUBLIC = 1,
}

export enum UserJoinRequestStatus {
  PENDING = 0,
  APPROVED = 1,
  REJECTED = 2,
}
